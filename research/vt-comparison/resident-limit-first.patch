--- before/packages/renderer-webgl/src/virtual-texture/runtime.ts
+++ after/packages/renderer-webgl/src/virtual-texture/runtime.ts
@@ -1474,6 +1474,7 @@
       gpu.atlas.slots,
       gpu.atlas.lastUsedFrames,
       this.#protectedPoolPages,
+      gpu.residentSlots.size >= gpu.maxResidentPages,
     );
   }
 
--- before/packages/renderer-webgl/src/virtual-texture/residency.ts
+++ after/packages/renderer-webgl/src/virtual-texture/residency.ts
@@ -51,6 +51,7 @@
   slots: readonly (VirtualTexturePoolSlot | undefined)[],
   lastUsedFrames: ArrayLike<number>,
   protectedPages: ProtectedVirtualTexturePoolPages,
+  ownSlotsOnly = false,
 ): number => {
   if (slots.length === 0 || slots.length !== lastUsedFrames.length) {
     throw new Error("Royal VT pool slots and recency storage must have equal non-zero length");
@@ -59,6 +60,7 @@
   let oldestFrame = Infinity;
   for (let slot = 0; slot < slots.length; slot += 1) {
     const resident = slots[slot];
+    if (ownSlotsOnly && resident?.resourceKey !== resourceKey) continue;
     if (resident?.resourceKey === resourceKey && resident.pageKey === pageKey) {
       return slot;
     }
